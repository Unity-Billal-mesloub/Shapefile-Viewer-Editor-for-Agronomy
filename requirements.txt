# Runtime dependencies for Shapefile-Viewer-Editor-for-Agronomy
# pip equivalent of pixi.toml, for users who are not on pixi/conda.
# Requires Python 3.12 (matches pixi.toml).
#
#   python -m venv .venv
#   .venv\Scripts\activate        # Windows
#   source .venv/bin/activate     # Linux / macOS
#   pip install -r requirements.txt
#   python main.py
#
# Note: geopandas pulls in shapely, pyproj and fiona as wheels on pip.
# On Windows, prefer Python 3.12 x64 so prebuilt wheels are available.

PySide6>=6.6.0,<7
geopandas>=1.1.1,<2
contextily>=1.6.2,<2
matplotlib>=3.8.0,<4
pandas>=2.0
